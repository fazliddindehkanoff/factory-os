# Тест-режим: проверка mini app по ролям в нескольких окнах

Режим для QA: **не надо менять роль одному Telegram-номеру**. Seed создаёт отдельных
тестовых пользователей с разными `user_id` и ролями; каждое окно браузера — отдельный
пользователь со своей сессией (токен в `sessionStorage`, не в `localStorage`).

Матрица «кто что видит и какую кнопку может нажать» — [ROLE_MATRIX.md](ROLE_MATRIX.md)
(права) и таблица шагов ниже (статусы).

## Включение

Работает **только** при `NODE_ENV=development` + `ENABLE_DEV_AUTH=1`
(в production сервер с `ENABLE_DEV_AUTH=1` вообще не стартует, а
`/api/auth/dev` и `/api/dev/users` отвечают stealth-404 — панель DEV не рендерится).

```bash
npm run db:migrate
npm run seed:test        # идемпотентный — можно перезапускать
npm run dev              # backend :3100 (или PORT из .env)
```

## Тестовые пользователи (PIN у всех: `1234`)

| Логин | Телефон | Кто это | Роли (коды) |
|---|---|---|---|
| `zayavitel_01` | `+998900000001` | Заявитель (сотрудник отдела) — создаёт заявку, как в боевом маршруте | `requester` |
| `sklad_01` | `+998900000002` | Склад (кладовщик) — создаёт заявку, принимает товар, закрывает | `requester`, `warehouse_worker` |
| `nach_sklad_01` | `+998900000003` | Начальник склада — подтверждает заявку | `warehouse` |
| `snab_01` | `+998900000004` | Снабженец — КП, выбор поставщика | `procurement_manager` |
| `zamdir_01` | `+998900000005` | Зам. директора — одобряет | `deputy_director` |
| `gendir_01` | `+998900000006` | Ген. директор — одобряет | `director` |
| `founder_01` | `+998900000007` | Учредитель — финально утверждает | `owner` |
| `admin_01` | `+998900000008` | Админ — конструктор/пользователи, НЕ бизнес-согласующий | `admin` |
| `nach_snab_01` | `+998900000009` | Руководитель снабжения — распределяет заявки по снабженцам | `procurement_head` |
| `snab_otdel_01` | `+998900000010` | Снабжение (отдел) | `procurement` |
| `fin_01` | `+998900000011` | Финансист | `finance` |
| `nach_fin_01` | `+998900000012` | Руководитель финансов | `finance_head` |
| `fin_manager_01` | `+998900000013` | Финансовый менеджер | `finance_manager` |
| `buhgalter_01` | `+998900000014` | Бухгалтер | `accountant` |
| `nach_otdela_01` | `+998900000015` | Руководитель отдела | `dept_head` |
| `vnedrenie_01` | `+998900000016` | Руководитель внедрения | `operations_lead` |
| `auditor_01` | `+998900000017` | Аудитор — только чтение/проверка | `auditor` |
| `nabludatel_01` | `+998900000018` | Наблюдатель — только чтение | `observer` |

Первые семь — полный маршрут заявки из ТЗ; остальные покрывают все прочие
системные роли (`src/rbac/system-roles.ts`) для проверки видимости и прав.

Все живут в отдельном холдинге «Тестовый завод» (свой склад, отделы, материалы,
workflow) — боевые данные не задеваются, даже если сид случайно прогнать по общей базе.

## Как открыть несколько окон одновременно

URL-параметр `phone` пиновывает **это окно** к тестовому пользователю:

```
https://<host>/?phone=%2B998900000002
https://<host>/?phone=%2B998900000003
https://<host>/?phone=%2B998900000004
https://<host>/?phone=%2B998900000005
https://<host>/?phone=%2B998900000006
https://<host>/?phone=%2B998900000007
https://<host>/?phone=%2B998900000008
```

Старый формат `?user=sklad_01` и `?user=<телефон>` тоже поддерживается для
обратной совместимости. (`/test?phone=...` также работает — SPA отдаёт index.html
на любой путь.)

Токен кладётся в `sessionStorage`, поэтому 5–7 вкладок/окон одного браузера — это
5–7 независимых пользователей. Инкогнито/другие браузеры не нужны, но тоже работают.

Внутри приложения — плавающая кнопка **DEV** (справа снизу): панель со списком
тестовых пользователей, клик переключает пользователя текущего окна.

## Маршрут заявки (workflow «Тестовый маршрут (полный)»)

| # | Шаг (статус) | Кто действует | Кнопки |
|---|---|---|---|
| 0 | Создание | `sklad_01` | «Новая заявка» |
| 1 | Подтверждение начальника склада (`pending_approval`) | `nach_sklad_01` | Согласовать (PIN) / Отклонить |
| 2 | Закупка: КП и выбор поставщика (`procurement`) | `snab_01` | Добавить КП / Выбрать поставщика / Отклонить |
| 3 | Одобрение зам. директора (`pending_approval`) | `zamdir_01` | Согласовать (PIN) / Отклонить |
| 4 | Одобрение ген. директора (`pending_approval`) | `gendir_01` | Согласовать (PIN) / Отклонить |
| 5 | Утверждение учредителя (`pending_approval`) | `founder_01` | Согласовать (PIN) / Отклонить |
| 6 | Прибытие товара на склад (`delivery`) | `snab_01` | В процессе доставки — финальный шаг тестового маршрута |

После последнего шага успешная тестовая заявка получает терминальный статус `approved`.
Другие терминальные статусы: `closed`, `rejected`, `cancelled`, `archived`.

Сид сразу кладёт 3 заявки: свежую (шаг 1), «у снабженца» (шаг 2) и отклонённую —
чтобы списки/фильтры/видимость можно было проверять без подготовки.

## Что проверять в каждом окне

- видит ли пользователь заявку в списке/инбоксе (модель видимости: свои + участвует +
  роль в workflow; `requester` — только свои);
- какие кнопки доступны на текущем шаге (и что чужих кнопок НЕТ);
- меняется ли статус у всех окон после действия (автообновление ~30 сек или reload);
- SoD: автор не может согласовать свою заявку; закрыть может только автор;
- PIN: неверный PIN блокируется, верный `1234` проходит.

## E2E-автотесты этого режима

- `src/db/seed-test.test.ts` — идемпотентность сида + полный проход сценария ТЗ
  всеми семью пользователями + проверка «чужие кнопки недоступны»;
- `src/http/dev-users.test.ts` — stealth-404 в prod, список пользователей в dev,
  вход каждым пользователем.
