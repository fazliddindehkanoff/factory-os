-- FIXES.xlsx 2026-07-17, лист B: шаг «Исполнительный директор» убирается из
-- цепочки согласования. Шаг выключаем (enabled=false), как это делает админ в
-- конструкторе: движок пропускает выключенные шаги, история и подписи по уже
-- пройденным заявкам сохраняются; заявка, стоящая на этом шаге сейчас, после
-- очередного действия перейдёт на следующий включённый шаг.
UPDATE "workflow_steps"
SET "enabled" = false
WHERE "step_name" = 'Исполнительный директор';
